2.
ssh -l met default gateway / altijd 2 hoger 
sh ip int brief, voor ips interfaces, en sub nets
sh run, kan je alles zien wat connected is
show lldp nei / als niet enabled is, dan conf t : lldp run     in lldp nei : bij de port ID is local interfaces in de word.

show ip route connected : alleen netwerken die connected
ping om te pingen. ping met eindigt 255 voor alle ips.


1.
do sh vlan brief
spanning tree ; de multi layer switch is altijd root primary vlan 1-100
4096 / 8192 / 12288 / 16384 / 20480.

int ra fa 0.. - fa 0.. 
descr naam vlan
sw mode access
sw access vlan nr.

dhcp 
ip dhcp excluded-address 10.50.0.0 10.50.0.49
ip dhcp pool vlan50
default-router 10.50.0.1 dns-server 180.115.0.6
network 10.50.0.0 255.255.0.0
default gateway eindigt op 1
